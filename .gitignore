# Claude Code runtime state — no debe versionarse
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/scheduled_tasks.json
.claude/.ralph-loop.local.md
.claude/orchestrator-state.local.json

# Artefactos de scripts/preview-server.sh — se generan en la raíz de cada worktree
.preview.json
.preview-logs/
sterminal.db

# Secretos (feat-21): API keys reales — nunca versionadas. backend/.env.example es
# la plantilla versionada, sin valores reales.
.env
*.env
!*.env.example
